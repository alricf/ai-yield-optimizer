{
  "name": "ai-yield-optimizer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ai-yield-optimizer",
    "create-local": "graph create --node http://localhost:8020/ ai-yield-optimizer",
    "remove-local": "graph remove --node http://localhost:8020/ ai-yield-optimizer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ai-yield-optimizer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.95.0",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
